{
  "name": "e-wallet-sentiment-database",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate": "prisma generate --schema=./schema.prisma",
    "db:push": "prisma db push --schema=./schema.prisma",
    "db:migrate": "prisma migrate deploy --schema=./schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
